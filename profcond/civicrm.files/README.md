This directory contains resources shipped with the
com.joineryhq.profcond extension.

These files are copied to [civicrm.files] during extension install
or upgrade and only those copies are used directly at runtime.
